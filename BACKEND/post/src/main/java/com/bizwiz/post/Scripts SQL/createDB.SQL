-- If u wanna check the function of posts, you should run this script, this is to run it locally.

-- Create a database if it doesn't exist with the name "posts".
CREATE DATABASE IF NOT EXISTS post ;

-- Delete the database if it exists and it have's a problem.
DROP DATABASE IF EXISTS post;